{
    "auth_keys": {
        "default": {
            "type": "standard",
            "key": "${PKI_DEFAULT_AUTH_KEY}"
        }
    },
    "remotes": {
        "default": "${PKI_DEFAULT_HOST_PORT}"
    },
    "signing": {
        "default": {
            "auth_remote": {
                "remote": "default",
                "auth_key": "default"
            }
	}
    }
}
